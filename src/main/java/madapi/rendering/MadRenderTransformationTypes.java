package madapi.rendering;

/** Reference to different rendering types such as first-person, third-person, inventory item, etc. */
public enum MadRenderTransformationTypes
{
    NONE, DROPPED, INVENTORY, THIRDPERSONEQUIPPED
}
